version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    post_checkout:
      - git fetch --force --tags

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true
